"use strict";
let Names = [`Juned`, `Khan`, `Typescript`, `GIAICC`];
const message = " GIAICC is not only clear determination but stong commitment too. ";
for (let i = 0; i < Names.length; i++) {
    console.log(Names[i] + message);
}
;
